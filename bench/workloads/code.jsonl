{"id": "code/py-binsearch", "prompt": "# Python implementation of an iterative binary search over a sorted list,\n# returning the insertion point when the target is absent.\ndef binary_search(arr, target):\n"}
{"id": "code/py-lru", "prompt": "# A fixed-capacity LRU cache in pure Python using an OrderedDict.\nfrom collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity):\n"}
{"id": "code/c-matmul", "prompt": "/* Cache-blocked single-precision matrix multiply in C.\n * C = A * B, all row-major, dimensions M x K, K x N. */\n#include <stddef.h>\n\nvoid sgemm_blocked(const float *A, const float *B, float *C,\n                   size_t M, size_t N, size_t K, size_t block)\n{\n"}
{"id": "code/cuda-reduce", "prompt": "// CUDA kernel: warp-shuffle based sum reduction over a float array,\n// one block per partial sum, using __shfl_down_sync.\n#include <cuda_runtime.h>\n\n__global__ void reduce_sum(const float *__restrict__ in, float *__restrict__ out, int n)\n{\n"}
{"id": "code/rust-parser", "prompt": "// Rust: a small recursive-descent parser for arithmetic expressions\n// supporting + - * / and parentheses over f64 literals.\n#[derive(Debug, Clone, PartialEq)]\nenum Token {\n"}
{"id": "code/go-worker", "prompt": "// Go: a bounded worker pool that processes jobs from a channel and\n// collects results, with graceful shutdown via context.\npackage pool\n\nimport (\n\t\"context\"\n\t\"sync\"\n)\n\n"}
{"id": "code/sql-window", "prompt": "-- SQL: for each customer, compute their running total of order amounts\n-- and rank their orders by value, using window functions.\nSELECT\n"}
{"id": "code/ts-react-hook", "prompt": "// TypeScript: a React hook that debounces a value by a given delay,\n// cleaning up its timer on unmount.\nimport { useState, useEffect } from 'react';\n\nexport function useDebounced<T>(value: T, delayMs: number): T {\n"}
