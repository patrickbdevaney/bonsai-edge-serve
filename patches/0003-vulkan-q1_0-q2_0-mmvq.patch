diff --git a/ggml/src/ggml-vulkan/ggml-vulkan.cpp b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
index fd22aba11..86a552ac8 100644
--- a/ggml/src/ggml-vulkan/ggml-vulkan.cpp
+++ b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
@@ -3454,6 +3454,13 @@ static bool ggml_vk_matmul_int_shmem_support(const vk_device& device, const std:
         case GGML_TYPE_Q5_1:    block_a_size = std430_size({{16, 4}, {4, 4}, {fp2_size, fp2_align}});         break; // qs[16/4] + qh + dm(vec2)
         case GGML_TYPE_Q8_0:    block_a_size = std430_size({{32, 4}, {fp_size,  fp_align}});                  break; // qs[8] + dm
         case GGML_TYPE_MXFP4:   block_a_size = std430_size({{32, 4}, {fp_size,  fp_align}});                  break; // qs[8] + d
+        // Bonsai low-bit types. Absent from this switch they hit `default:
+        // return false`, which clears mul_mat_*_int[t], which stops
+        // CREATE_MMQ from making a pipeline, which makes the mat-mat lookup
+        // return nullptr and silently fall back to the dequant path. Four
+        // layers of "supported" all defaulting to no.
+        case GGML_TYPE_Q2_0:    block_a_size = std430_size({{ 8, 4}, {fp_size,  fp_align}});                  break; // qs[2] + d
+        case GGML_TYPE_Q1_0:    block_a_size = std430_size({{ 4, 4}, {fp_size,  fp_align}});                  break; // qs[1] + d
         case GGML_TYPE_Q2_K:    block_a_size = std430_size({{ 8, 4}, {2, 2}, {fp2_size, fp2_align}});         break; // qs[2] + scales(u8vec2) + dm(vec2)
         case GGML_TYPE_Q3_K:    block_a_size = std430_size({{16, 4}, {fp2_size, fp2_align}});                 break; // qs[4] + d_scales(vec2)
         case GGML_TYPE_Q4_K:    block_a_size = std430_size({{16, 4}, {fp2_size, fp2_align}});                 break; // qs[4] + dm(vec2)
@@ -4280,6 +4287,11 @@ static void ggml_vk_load_shaders(vk_device& device, vk_pipeline requested) {
 
 #if defined(GGML_VULKAN_INTEGER_DOT_GLSLC_SUPPORT)
         if (device->integer_dot_product) {
+            // Q2_0/Q1_0 (Bonsai). Registration is explicit per type -- generating
+            // the shader is not enough, the runtime silently falls back to the
+            // dequant matmul without this, which reads as "no speedup".
+            CREATE_MMQ(GGML_TYPE_Q2_0, pipeline_dequant_mul_mat_mat_q8_1[GGML_TYPE_Q2_0], matmul_q2_0_q8_1, mmq_wg_denoms, warptile_mmq_int, vk_mat_mat_push_constants, 3, , 0);
+            CREATE_MMQ(GGML_TYPE_Q1_0, pipeline_dequant_mul_mat_mat_q8_1[GGML_TYPE_Q1_0], matmul_q1_0_q8_1, mmq_wg_denoms, warptile_mmq_int, vk_mat_mat_push_constants, 3, , 0);
             CREATE_MMQ(GGML_TYPE_Q4_0, pipeline_dequant_mul_mat_mat_q8_1[GGML_TYPE_Q4_0], matmul_q4_0_q8_1, mmq_wg_denoms, warptile_mmq_int, vk_mat_mat_push_constants, 3, , 0);
             CREATE_MMQ(GGML_TYPE_Q4_1, pipeline_dequant_mul_mat_mat_q8_1[GGML_TYPE_Q4_1], matmul_q4_1_q8_1, mmq_wg_denoms, warptile_mmq_int, vk_mat_mat_push_constants, 3, , 0);
             CREATE_MMQ(GGML_TYPE_Q5_0, pipeline_dequant_mul_mat_mat_q8_1[GGML_TYPE_Q5_0], matmul_q5_0_q8_1, mmq_wg_denoms, warptile_mmq_int, vk_mat_mat_push_constants, 3, , 0);
@@ -4652,6 +4664,11 @@ static void ggml_vk_load_shaders(vk_device& device, vk_pipeline requested) {
                 const uint32_t subgroup_size_int = (device->vendor_id == VK_VENDOR_ID_INTEL && device->subgroup_size_control) ? device->subgroup_min_size : device->subgroup_size;
                 const uint32_t wg_size_subgroup_int = (w == DMMV_WG_SIZE_SUBGROUP) ? subgroup_size_int : (subgroup_size_int * 4);
 
+                // Q2_0 (Bonsai ternary): integer-dot MMVQ path. Registration is
+                // explicit per type -- generating the shader is not enough, the
+                // runtime silently falls back to dequant-MMV without this line.
+                ggml_vk_create_pipeline(device, device->pipeline_dequant_mul_mat_vec_q8_1_f32[w][GGML_TYPE_Q1_0][i], "mul_mat_vec_q1_0_q8_1_f32", arr_dmmv_q1_0_q8_1_f32_len[reduc], arr_dmmv_q1_0_q8_1_f32_data[reduc], "main", mul_mat_vec_num_bindings, sizeof(vk_mat_vec_push_constants), {1*rm_stdq_int, 1, 1}, {wg_size_subgroup_int, 1*rm_stdq_int, i+1}, 1, true, use_subgroups, subgroup_size_int);
+                ggml_vk_create_pipeline(device, device->pipeline_dequant_mul_mat_vec_q8_1_f32[w][GGML_TYPE_Q2_0][i], "mul_mat_vec_q2_0_q8_1_f32", arr_dmmv_q2_0_q8_1_f32_len[reduc], arr_dmmv_q2_0_q8_1_f32_data[reduc], "main", mul_mat_vec_num_bindings, sizeof(vk_mat_vec_push_constants), {1*rm_stdq_int, 1, 1}, {wg_size_subgroup_int, 1*rm_stdq_int, i+1}, 1, true, use_subgroups, subgroup_size_int);
                 ggml_vk_create_pipeline(device, device->pipeline_dequant_mul_mat_vec_q8_1_f32[w][GGML_TYPE_Q4_0][i], "mul_mat_vec_q4_0_q8_1_f32", arr_dmmv_q4_0_q8_1_f32_len[reduc], arr_dmmv_q4_0_q8_1_f32_data[reduc], "main", mul_mat_vec_num_bindings, sizeof(vk_mat_vec_push_constants), {1*rm_stdq_int, 1, 1}, {wg_size_subgroup_int, 1*rm_stdq_int, i+1}, 1, true, use_subgroups, subgroup_size_int);
                 ggml_vk_create_pipeline(device, device->pipeline_dequant_mul_mat_vec_q8_1_f32[w][GGML_TYPE_Q4_1][i], "mul_mat_vec_q4_1_q8_1_f32", arr_dmmv_q4_1_q8_1_f32_len[reduc], arr_dmmv_q4_1_q8_1_f32_data[reduc], "main", mul_mat_vec_num_bindings, sizeof(vk_mat_vec_push_constants), {1*rm_stdq_int, 1, 1}, {wg_size_subgroup_int, 1*rm_stdq_int, i+1}, 1, true, use_subgroups, subgroup_size_int);
                 ggml_vk_create_pipeline(device, device->pipeline_dequant_mul_mat_vec_q8_1_f32[w][GGML_TYPE_Q5_0][i], "mul_mat_vec_q5_0_q8_1_f32", arr_dmmv_q5_0_q8_1_f32_len[reduc], arr_dmmv_q5_0_q8_1_f32_data[reduc], "main", mul_mat_vec_num_bindings, sizeof(vk_mat_vec_push_constants), {1*rm_stdq_int, 1, 1}, {wg_size_subgroup_int, 1*rm_stdq_int, i+1}, 1, true, use_subgroups, subgroup_size_int);
@@ -6868,6 +6885,14 @@ static vk_pipeline ggml_vk_get_dequantize_mul_mat_vec(ggml_backend_vk_context *
 
     if (b_type == GGML_TYPE_Q8_1) {
         switch (a_type) {
+            // Q2_0 (Bonsai ternary). Omitting a type here is a SILENT
+            // fallback, not an error: the caller sees nullptr and quietly
+            // reverts to the f32 dequant path (ggml_vk_mul_mat_q_f16,
+            // "Fall back to f16 dequant mul mat"). The symptom is therefore
+            // unchanged tok/s rather than a crash -- indistinguishable from
+            // "the kernel dispatched and is simply no faster".
+            case GGML_TYPE_Q1_0:
+            case GGML_TYPE_Q2_0:
             case GGML_TYPE_Q4_0:
             case GGML_TYPE_Q4_1:
             case GGML_TYPE_Q5_0:
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
index 6fe3e2dc0..197767ab0 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
@@ -14,7 +14,20 @@ layout(local_size_x_id = 0, local_size_y = 1, local_size_z = 1) in;
 #define K_PER_ITER 8
 #elif defined(DATA_A_QUANT_K)
 #define K_PER_ITER 16
-#elif defined(DATA_A_IQ1_S) || defined(DATA_A_IQ1_M)
+#elif defined(DATA_A_IQ1_S) || defined(DATA_A_IQ1_M) || defined(DATA_A_Q2_0) || defined(DATA_A_Q1_0)
+// Q1_0/Q2_0 take the 32-quant class so each call issues a WIDER load: 32
+// quants is 8 bytes for Q2_0 (two consecutive uint32) and 4 bytes for Q1_0,
+// against 4 and 2 bytes in the 16-quant class.
+//
+// Measured on Thor (kernels/vulkan/bandwidth_vk.cpp), streaming read:
+//   one 32-bit word per lane, lane-strided   180.6 GB/s
+//   two consecutive words per lane           227.9 GB/s
+//   four consecutive / uvec4                 241-249 GB/s
+// The driver merges consecutive per-lane loads into wide transactions; it
+// does not do that for the lane-strided pattern. Four words would be better
+// still but spans two q8_1 blocks, and this framework carries one b-scale
+// per call, so 32 quants is the most that fits without changing the
+// contract for every other type.
 #define K_PER_ITER 32
 #else
 #error unimplemented
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq_funcs.glsl b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq_funcs.glsl
index 73cf9c799..045c57d1c 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq_funcs.glsl
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq_funcs.glsl
@@ -29,6 +29,100 @@ FLOAT_TYPEV2 get_dm(uint ib) {
 }
 #endif
 
+#if defined(DATA_A_Q1_0)
+// Q1_0 (Bonsai 1-bit): 128 quants per 18-byte block, one bit each. `ib` is
+// a virtual index in units of 32 quants (QUANT_K_Q8_1) -> real block ib/4.
+FLOAT_TYPE get_dm(uint ib) {
+    return FLOAT_TYPE(data_a[ib / 4].d);
+}
+
+// Bit order is LSB-first and consecutive: weight j lives at byte j/8, bit
+// j%8 (see dequantize_row_q1_0 in ggml-quants.c). Spread 4 bits to 4 bytes
+// with one multiply: bit i must land at bit 8i, so scaling by
+// 1 + 2^7 + 2^14 + 2^21 = 0x00204081 places each and the mask drops the
+// cross terms. Verified exhaustively against the shift/or form over all
+// 65536 x 4 inputs -- 0 mismatches.
+int32_t q1_0_codes(uint h, uint p) {
+    const uint n = (h >> (4u * p)) & 0xFu;
+    return int32_t((n * 0x00204081u) & 0x01010101u);
+}
+
+FLOAT_TYPE mmvq_dot_product(const uint ib_a, const uint iqs) {
+    // 32 quants = one 32-bit word, assembled from two 16-bit loads. A Q1_0
+    // block is 2 + 16 = 18 bytes and only 2-byte aligned, so a uint32_t
+    // member would pad the std430 stride and misread every block.
+    const uint wi = (ib_a % 4) * 2;
+    const uint w = pack32(u16vec2(data_a_packed16[ib_a / 4].qs[wi    ],
+                                  data_a_packed16[ib_a / 4].qs[wi + 1]));
+
+    int32_t q_sum = 0;
+    for (uint k = 0; k < 8; ++k) {
+        q_sum += dotPacked4x8EXT(q1_0_codes(w, k), cache_b_qs[k]);
+    }
+
+    // Bits {0,1} represent values {-1,+1}, i.e. v = 2c - 1, so
+    // sum(v*b) = 2*sum(c*b) - sum(b). One call now covers the whole 32-quant
+    // q8_1 block, so the correction takes all of cache_b_ds.y rather than a
+    // fraction of it.
+    return FLOAT_TYPE(float(get_dm(ib_a))
+                      * (2.0 * float(q_sum) * cache_b_ds.x - cache_b_ds.y));
+}
+#endif
+
+#if defined(DATA_A_Q2_0)
+// Q2_0 (Bonsai ternary): 128 quants per 34-byte block. `ib` is a virtual
+// index in units of 32 quants (QUANT_K_Q8_1), so the real block is ib/4.
+FLOAT_TYPE get_dm(uint ib) {
+    return FLOAT_TYPE(data_a[ib / 4].d);
+}
+
+// GGUF order: byte p of the word holds logical weights 4p..4p+3 at shifts
+// 0,2,4,6. Q2_K's `(w >> s) & 0x03030303` does NOT work here -- it takes
+// the same field from each byte, i.e. weights strided by 4, silently paired
+// with the wrong activations. Extract per byte instead. Validated against a
+// CPU reference in bonsai-edge-serve (kernels/vulkan/gemv_q2.comp,
+// -DGGUF_ORDER): max rel err 4.783e-06.
+//
+// The multiply trick used for Q1_0 does NOT port: 2-bit fields carry into
+// each other and 96 of 256 byte values disagree. Checked exhaustively.
+int32_t q2_0_codes(uint w, uint p) {
+    const uint c4 = (w >> (8u * p)) & 0xFFu;
+    return int32_t( (c4        & 3u)
+                 | (((c4 >> 2) & 3u) <<  8)
+                 | (((c4 >> 4) & 3u) << 16)
+                 | (((c4 >> 6) & 3u) << 24));
+}
+
+FLOAT_TYPE mmvq_dot_product(const uint ib_a, const uint iqs) {
+    // 32 quants = TWO CONSECUTIVE 32-bit words. Issued back to back so the
+    // driver can merge them: 227.9 GB/s against 180.6 for one word per lane
+    // (kernels/vulkan/bandwidth_vk.cpp). A Q2_0 block is 34 bytes and only
+    // 2-byte aligned, hence the packed16 view -- a uint32_t member pads the
+    // std430 stride to 36 and misreads every block after the first.
+    const uint wi = (ib_a % 4) * 4;
+    const uint16_t h0 = data_a_packed16[ib_a / 4].qs[wi    ];
+    const uint16_t h1 = data_a_packed16[ib_a / 4].qs[wi + 1];
+    const uint16_t h2 = data_a_packed16[ib_a / 4].qs[wi + 2];
+    const uint16_t h3 = data_a_packed16[ib_a / 4].qs[wi + 3];
+    const uint w0 = pack32(u16vec2(h0, h1));
+    const uint w1 = pack32(u16vec2(h2, h3));
+
+    int32_t q_sum = 0;
+    for (uint p = 0; p < 4; ++p) {
+        q_sum += dotPacked4x8EXT(q2_0_codes(w0, p), cache_b_qs[p]);
+    }
+    for (uint p = 0; p < 4; ++p) {
+        q_sum += dotPacked4x8EXT(q2_0_codes(w1, p), cache_b_qs[4 + p]);
+    }
+
+    // Codes {0,1,2,3} represent values {-1,0,1,2}, so sum(v*b) =
+    // sum(c*b) - sum(b). One call now covers the whole 32-quant q8_1 block,
+    // so the correction takes all of cache_b_ds.y.
+    return FLOAT_TYPE(float(get_dm(ib_a))
+                      * (float(q_sum) * cache_b_ds.x - cache_b_ds.y));
+}
+#endif
+
 // Each iqs value maps to a 32-bit integer
 #if defined(DATA_A_Q4_0)
 // 2-byte loads for Q4_0 blocks (18 bytes)
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_funcs.glsl b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_funcs.glsl
index 59931b04b..a1d58e0a5 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_funcs.glsl
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_funcs.glsl
@@ -185,6 +185,81 @@ ACC_TYPE mmq_dot_product(const uint ib_a) {
 
 // For k-quants, ib and iqs still assume 32-wide blocks, but k-quants are 256-wide
 // iqs still refers to a 32-bit integer, meaning 0..7 for 32-wide quants
+#if defined(DATA_A_Q2_0)
+// Q2_0 (Bonsai ternary). `ib` counts 32-quant units, so the real 34-byte
+// block is ib/4 and the sub-block is ib%4. packed16 because a 34-byte block
+// is only 2-byte aligned -- a uint32_t member pads the std430 stride to 36
+// and misreads every block after the first.
+void block_a_to_shmem(const uint buf_ib, const uint ib, const uint iqs) {
+    const uint wi = ((ib % 4) * 2 + iqs) * 2;
+    buf_a[buf_ib].qs[iqs] = pack32(u16vec2(data_a_packed16[ib / 4].qs[wi    ],
+                                           data_a_packed16[ib / 4].qs[wi + 1]));
+    if (iqs == 0) {
+        buf_a[buf_ib].dm = FLOAT_TYPE(data_a[ib / 4].d);
+    }
+}
+
+void block_a_to_registers(const uint reg_ib, const uint buf_ib) {
+    cache_a[reg_ib].dm    = buf_a[buf_ib].dm;
+    cache_a[reg_ib].qs[0] = buf_a[buf_ib].qs[0];
+    cache_a[reg_ib].qs[1] = buf_a[buf_ib].qs[1];
+}
+
+// GGUF order: byte p of the word holds logical weights 4p..4p+3 at shifts
+// 0,2,4,6. Q2_K's `(w >> s) & 0x03030303` selects the same field from each
+// byte -- weights strided by 4 -- and would pair them with the wrong
+// activations while still producing plausible numbers.
+ACC_TYPE mmq_dot_product(const uint ib_a) {
+    int32_t q_sum = 0;
+    [[unroll]] for (uint iqs = 0; iqs < 8; iqs++) {
+        const uint w  = cache_a[ib_a].qs[iqs / 4];
+        const uint c4 = (w >> (8u * (iqs % 4))) & 0xFFu;
+        const int32_t codes = int32_t( (c4        & 3u)
+                                    | (((c4 >> 2) & 3u) <<  8)
+                                    | (((c4 >> 4) & 3u) << 16)
+                                    | (((c4 >> 6) & 3u) << 24));
+        q_sum += dotPacked4x8EXT(codes, cache_b.qs[iqs]);
+    }
+    // codes {0,1,2,3} are values {-1,0,1,2}: sum(v*b) = sum(c*b) - sum(b),
+    // and this call covers the whole 32-quant q8_1 block, so the correction
+    // takes all of ds.y.
+    return ACC_TYPE(float(cache_a[ib_a].dm)
+                    * (float(q_sum) * float(cache_b.ds.x) - float(cache_b.ds.y)));
+}
+#endif
+
+#if defined(DATA_A_Q1_0)
+// Q1_0 (Bonsai 1-bit), 18-byte block -> packed16 for the same reason.
+void block_a_to_shmem(const uint buf_ib, const uint ib, const uint iqs) {
+    const uint wi = (ib % 4) * 2;
+    buf_a[buf_ib].qs[0] = pack32(u16vec2(data_a_packed16[ib / 4].qs[wi    ],
+                                         data_a_packed16[ib / 4].qs[wi + 1]));
+    buf_a[buf_ib].dm = FLOAT_TYPE(data_a[ib / 4].d);
+}
+
+void block_a_to_registers(const uint reg_ib, const uint buf_ib) {
+    cache_a[reg_ib].dm    = buf_a[buf_ib].dm;
+    cache_a[reg_ib].qs[0] = buf_a[buf_ib].qs[0];
+}
+
+ACC_TYPE mmq_dot_product(const uint ib_a) {
+    const uint w = cache_a[ib_a].qs[0];
+    int32_t q_sum = 0;
+    // Spread 4 bits across 4 bytes with one multiply: bit i must land at
+    // bit 8i, so scaling by 1 + 2^7 + 2^14 + 2^21 places each and the mask
+    // drops the cross terms. Verified exhaustively over all 65536 x 4
+    // inputs against the shift/or form.
+    [[unroll]] for (uint iqs = 0; iqs < 8; iqs++) {
+        const uint n = (w >> (4u * iqs)) & 0xFu;
+        q_sum += dotPacked4x8EXT(int32_t((n * 0x00204081u) & 0x01010101u),
+                                 cache_b.qs[iqs]);
+    }
+    // bits {0,1} are values {-1,+1}: v = 2c - 1.
+    return ACC_TYPE(float(cache_a[ib_a].dm)
+                    * (2.0 * float(q_sum) * float(cache_b.ds.x) - float(cache_b.ds.y)));
+}
+#endif
+
 #if defined(DATA_A_Q2_K)
 // 4-byte loads for Q2_K blocks (84 bytes)
 void block_a_to_shmem(const uint buf_ib, const uint ib, const uint iqs) {
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_shmem_types.glsl b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_shmem_types.glsl
index 79c933f40..54cc50f3c 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_shmem_types.glsl
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_shmem_types.glsl
@@ -53,6 +53,23 @@ struct block_a_cache {
     int32_t qs[8];
     FLOAT_TYPE d;
 };
+#elif defined(DATA_A_Q2_0)
+// 4 quants per byte, so LOAD_VEC_A = 16 and iqs is 0 or 1 across BK=32.
+// Each iqs step stores one 32-bit word = 16 quants, in GGUF order; the
+// unpack happens in mmq_dot_product.
+#define QUANT_R_MMQ 4
+struct block_a_cache {
+    uint32_t qs[2];
+    FLOAT_TYPE dm;
+};
+#elif defined(DATA_A_Q1_0)
+// 8 quants per byte -> LOAD_VEC_A = 32 = BK, so iqs is always 0 and one
+// 32-bit word carries the whole 32-quant unit.
+#define QUANT_R_MMQ 8
+struct block_a_cache {
+    uint32_t qs[1];
+    FLOAT_TYPE dm;
+};
 #elif defined(DATA_A_Q2_K)
 #define QUANT_R_MMQ 4
 struct block_a_cache {
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/types.glsl b/ggml/src/ggml-vulkan/vulkan-shaders/types.glsl
index f31d79a61..07a9b65f2 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/types.glsl
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/types.glsl
@@ -199,11 +199,20 @@ struct block_q1_0
     uint8_t qs[QUANT_K_Q1_0 / 8];
 };
 
+// 2 + 16 = 18 bytes, 2-byte aligned -- same constraint as Q4_0, see the
+// note on block_q2_0_packed16 below.
+struct block_q1_0_packed16
+{
+    float16_t d;
+    uint16_t qs[QUANT_K_Q1_0 / 16];
+};
+
 #if defined(DATA_A_Q1_0)
 #define QUANT_K QUANT_K_Q1_0
 #define QUANT_R QUANT_R_Q1_0
 #define QUANT_AUXF 1
 #define A_TYPE block_q1_0
+#define A_TYPE_PACKED16 block_q1_0_packed16
 #endif
 
 #define QUANT_K_Q2_0 128
@@ -215,11 +224,22 @@ struct block_q2_0
     uint8_t qs[QUANT_K_Q2_0 / 4];
 };
 
+// A Q2_0 block is 2 + 32 = 34 bytes and therefore only 2-byte aligned.
+// A uint32_t member would force std430 alignment 4, padding the array
+// stride to 36 and misreading every block after the first. Q4_0 (18
+// bytes) uses a packed16 view for exactly this reason.
+struct block_q2_0_packed16
+{
+    float16_t d;
+    uint16_t qs[QUANT_K_Q2_0 / 8];
+};
+
 #if defined(DATA_A_Q2_0)
 #define QUANT_K QUANT_K_Q2_0
 #define QUANT_R QUANT_R_Q2_0
 #define QUANT_AUXF 1
 #define A_TYPE block_q2_0
+#define A_TYPE_PACKED16 block_q2_0_packed16
 #endif
 
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp b/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
index fc0dd01c4..a4825fe2e 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
@@ -591,7 +591,7 @@ void matmul_shaders(bool fp16, MatMulIdType matmul_id_type, bool coopmat, bool c
 
 #if defined(GGML_VULKAN_INTEGER_DOT_GLSLC_SUPPORT)
         // Integer dot mmq performs better with f32 accumulators
-        if (!f16acc && !coopmat && !coopmat2 && (is_legacy_quant(tname) || is_k_quant(tname) || tname == "mxfp4")) {
+        if (!f16acc && !coopmat && !coopmat2 && (is_legacy_quant(tname) || is_k_quant(tname) || tname == "mxfp4" || tname == "q2_0" || tname == "q1_0")) {
             string_to_spv(shader_name + "_" + tname + "_q8_1", "mul_mmq.comp", merge_maps(merge_maps(base_dict, float_type_dict), {{data_a_key, "1"}, {"D_TYPE", "float"},}), fp16, coopmat, coopmat2, f16acc);
         }
 #endif
@@ -707,7 +707,7 @@ void process_shaders() {
 
         // mul mat vec with integer dot product
 #if defined(GGML_VULKAN_INTEGER_DOT_GLSLC_SUPPORT)
-        if (is_legacy_quant(tname) || tname == "mxfp4" || is_k_quant(tname) || tname == "iq1_s" || tname == "iq1_m") {
+        if (is_legacy_quant(tname) || tname == "mxfp4" || is_k_quant(tname) || tname == "iq1_s" || tname == "iq1_m" || tname == "q2_0" || tname == "q1_0") {
             string_to_spv("mul_mat_vec_" + tname + "_q8_1_f32", "mul_mat_vecq.comp", merge_maps(base_dict, {{data_a_key, "1"}, {"D_TYPE", "float"}, {"FLOAT_TYPE", "float"}, {"FLOAT_TYPEV2", "vec2"}, {"ACC_TYPE", "float"}}));
             string_to_spv("mul_mat_vec_" + tname + "_q8_1_f32_subgroup", "mul_mat_vecq.comp", merge_maps(base_dict, {{data_a_key, "1"}, {"D_TYPE", "float"}, {"FLOAT_TYPE", "float"}, {"FLOAT_TYPEV2", "vec2"}, {"ACC_TYPE", "float"}, {"USE_SUBGROUP_ADD", "1"}}));
             string_to_spv("mul_mat_vec_" + tname + "_q8_1_f32_subgroup_no_shmem", "mul_mat_vecq.comp", merge_maps(base_dict, {{data_a_key, "1"}, {"D_TYPE", "float"}, {"FLOAT_TYPE", "float"}, {"FLOAT_TYPEV2", "vec2"}, {"ACC_TYPE", "float"}, {"USE_SUBGROUP_ADD_NO_SHMEM", "1"}}));
@@ -1147,7 +1147,7 @@ void write_output_files() {
 
     for (const std::string& btype : btypes) {
     for (const auto& tname : type_names) {
-        if (btype == "q8_1" && !is_legacy_quant(tname) && tname != "mxfp4" && !is_k_quant(tname) && tname != "iq1_s" && tname != "iq1_m") {
+        if (btype == "q8_1" && !is_legacy_quant(tname) && tname != "mxfp4" && !is_k_quant(tname) && tname != "iq1_s" && tname != "iq1_m" && tname != "q2_0" && tname != "q1_0") {
             continue;
         }
         hdr << "extern const void * arr_dmmv_"   << tname << "_" << btype << "_f32_data[3];\n";
